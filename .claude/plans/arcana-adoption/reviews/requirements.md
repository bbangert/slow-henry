## Requirements Coverage (from .claude/plans/arcana-adoption/plan.md)

| # | Requirement | Status | Evidence |
|---|-------------|--------|----------|
| 0.1 | Verify locked bumblebee supports `cross_encoding/3` w/ ms-marco-MiniLM-L-6-v2 | MET | `lib/retrieval_node/reranking/nx_serving_impl.ex` implements serving on this model; plan notes PASS at bumblebee 0.7.0, no bump — no diff evidence of a version bump needed, consistent |
| 0.1-verify | Embedding regression test untouched | MET | no changes to `nx_serving_impl.ex` embedding path in diff files list |
| 1.1 | `RetrievalNode.Reranking` serving (behaviour+NxServingImpl+stub), supervised, test-disabled, passage truncation | MET | `lib/retrieval_node/reranking/{serving,supervisor,warmer,nx_serving_impl}.ex`; `application.ex:61,69-70` gates supervisor via `reranking_serving_start?` (default true, off in :test config); `nx_serving_impl.ex:58-59` `truncate_passage/1` |
| 1.2 | Wire rerank into search entry point, top-50 candidate pool, `rerank` opt, preserve scores | MET-with-note | Implemented in `Search.hybrid_search/2` not `Search.semantic/2` as plan text said (plan itself flags this, ACCEPTED deviation); `search.ex:38-52,60-63,111-118` — `fused_score` preserved, `rerank_candidates` config default 50 |
| 1.3 | MCP `semantic_search` optional `rerank` field | MET | `lib/retrieval_node/mcp/tools/semantic_search.ex:32` `field(:rerank, :boolean, ...)`, `:53` plumbed, `fused_score` conditionally emitted `:87-88` |
| 1.4 | Eval gate bench script, ≥20 query pairs, MRR/Hit@k + p50/p95, decision to flip default | MET (gate FAILED → default false, per spec) | `lib/retrieval_node/bench/rerank_eval.ex`, `mix.exs`/`lib/mix/tasks/rn.rerank_eval.ex`; `priv/bench/queries.jsonl` = 24 lines (≥20); `config/config.exs:101` `rerank_default: false` — matches documented FAILED gate (MRR 0.320 vs 0.323, p95 2794ms) |
| 2.1 | Migration: entities/entity_mentions/entity_edges, cascades, uniques, trigram GIN | MET | `priv/repo/migrations/20260714140001_create_graph_tables.exs` — all three tables, `on_delete: :delete_all` on every FK, unique indexes on `(source_id,language,qualified_name)`, `(entity_id,chunk_id,kind)`, `(source_entity_id,target_entity_id,kind)`, trigram GIN index created via `execute/2` |
| 2.2 | `Graph.Extractor` behaviour + TreeSitter impl, single-parse reuse, per-language node maps | MET | `lib/retrieval_node/graph/extractor.ex`, `lib/retrieval_node/graph/extractor/tree_sitter.ex`; `lib/retrieval_node/chunking.ex:55-90` `chunk_with_graph/2` optional callback + dispatcher w/ `Code.ensure_loaded?` guard (bug fix noted in plan) |
| 2.3 | Persistence: stage entities/refs through PendingChunks into UpsertChunks transaction, param-limit batching | MET | `priv/repo/migrations/20260714140002_add_graph_to_pending_chunks.exs`; `lib/retrieval_node/graph.ex:32-38,51-56` `upsert_from_staged/3` batches at `@insert_batch_size 2_000`; `lib/retrieval_node/ingest/workers/upsert_chunks.ex:46-52,63` calls `Graph.upsert_from_staged` inside same Multi |
| 2.4 | Lifecycle: cascade test + periodic GC of zero-mention entities | MET | `test/retrieval_node/graph/graph_test.exs:381-413` cascade + GC tests; `lib/retrieval_node/ingest/workers/graph_gc.ex` Oban worker (queue `:upsert`); orphan gap documented in moduledoc |
| 2.5 | Backfill mix task, run against 5433, record duration/counts | PARTIAL (code done, execution blocked) | `lib/mix/tasks/rn.graph.backfill.ex` fully implemented with `--status`; no evidence in scratchpad of actual 5433 run/duration recorded — blocked per known dev-DB permission issue |
| 2.5-verify | Full suite + psql spot-checks | UNCLEAR | cannot execute in this environment (DB connection unavailable here); no scratchpad transcript found to confirm |
| 3.1 | Third CTE leg `entity_search`, trigram match, mention-kind weighting, RRF union w/ inlined filters | MET | `lib/retrieval_node/search/hybrid_query.ex:147-160,197-207` `entity_search` CTE, weighted RRF `SUM(weight/(k+rank))`, byte-identical two-leg SQL when off (`:264,280-281`) |
| 3.2 | Config weight for graph leg, env-tunable, conservative default | MET | `config/config.exs:107` `graph_leg_weight: 0.5`; `hybrid_query.ex:280` reads via `Application.get_env` |
| 3.3 | EXPLAIN ANALYZE index verification on 5433 | PARTIAL (blocked per plan) | no EXPLAIN output found in diff/scratchpad; plan marks this blocked on dev-DB migration+backfill — consistent with known blocker |
| 3.3-verify | Full suite + latency comparison | UNCLEAR | cannot execute here |
| 4.1 | `related_code` MCP tool: entity/repo/lang/relation/hops fields, entities+snippets w/ provenance | MET | `lib/retrieval_node/mcp/tools/related_code.ex:28,46-52,57-126` relation map, hops validation 1-2, `build_payload` for definitions/related entities |
| 4.2 | `semantic_search` `graph` boolean opt, config default, description updated | MET | `lib/retrieval_node/mcp/tools/semantic_search.ex:12-13,37-39` field + description |
| 4.3 | Live smoke via MCP HTTP transport, transcript recorded | PARTIAL (blocked per plan) | no transcript found; plan marks blocked on dev server restart + backfill — consistent with known blocker |
| 4.3-verify | Full suite + `/healthz` green | UNCLEAR | cannot execute here |
| 5.1 | `Graph.Extractor.LLM` stub, `{:error, :not_configured}`, moduledoc pointing at jira.ex Req/Finch pattern | MET | `lib/retrieval_node/graph/extractor/llm.ex` — exact stub, moduledoc references `RetrievalNode.Ingest.Jira` pattern |
| Risk: param-limit batching (PR #14 lessons) | Applied in graph persistence | MET | `lib/retrieval_node/graph.ex:32-38` comment explicitly ties batch size to PendingChunks/UpsertChunks precedent; `upsert_chunks.ex:46-52` same pattern |
| Risk: cascade FKs from chunks.id | Applied to new graph tables | MET | migration `20260714140001` — `entity_mentions.chunk_id` `on_delete: :delete_all`, entities/edges cascade from source/entities too |

**Summary**: 17 MET · 3 PARTIAL · 0 UNMET · 4 UNCLEAR
